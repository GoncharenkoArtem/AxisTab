<Window x:Class="TSODD.forms.GroupsAddForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TSODD.forms"
        mc:Ignorable="d"
        Title="Группы" Height="450" Width="335" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height= "40"></RowDefinition>
            <RowDefinition Height= "30"></RowDefinition>
            <RowDefinition Height= "*"></RowDefinition>
            <RowDefinition Height= "30"></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Margin="10,0,0,0" Grid.Row="0">
            <TextBlock Text="Тип элементов:" VerticalAlignment="Center" ></TextBlock>
            <ComboBox x:Name="cb_TypeOfElements" Height="22" Width="100" Margin="10,0,0,0" HorizontalContentAlignment="Center" SelectionChanged="cb_TypeOfElements_SelectionChanged">
                <TextBlock Text="знаки"></TextBlock>
                <TextBlock Text="разметка"></TextBlock>
            </ComboBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="10,0,0,0"  Grid.Row="1">
            <TextBlock Text="Имя группы:" VerticalAlignment="Center" ></TextBlock>
            <TextBox x:Name="tb_GroupName" Height="22" Width="100" Margin="25,0,0,0"  HorizontalContentAlignment="Center" VerticalContentAlignment="Center" ></TextBox>
            <Button x:Name="bt_AddGroupName" Content="Добавить" Height="22" Width="95" Margin="10,0,0,0" Click="bt_AddGroupName_Click"></Button>
        </StackPanel>

        <DataGrid   x:Name="gridGroups"
             AutoGenerateColumns="False"
             CanUserAddRows="False"
             Width="300"
             Height="290"
             Grid.Row="2"
                 
             RowBackground="White"
  
             SelectionMode="Extended"
             SelectionUnit="FullRow"
             HeadersVisibility="Column"
             HorizontalScrollBarVisibility="Disabled"
    
             BorderBrush="Gray"
 
             BorderThickness="1.5"
             CanUserSortColumns="False" 
             EnableColumnVirtualization="True">

            <!-- Стили заголовков колонок -->
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Height" Value="20"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="BorderBrush" Value="Gray"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1.5"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <!-- Стили строк -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="Height" Value="22"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderBrush" Value="Gray"  ></Setter>
                    <Setter Property="BorderThickness" Value="0,0,0,0"/>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>

                <!-- Имя -->
                <DataGridTemplateColumn Header="Группы" Width="6*" IsReadOnly ="True" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"  Margin="10,0,0,0" VerticalAlignment="Center"></TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                
                <!-- Button Delete -->
                <DataGridTemplateColumn Header="" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button  Margin="14,2,12,2" Name ="offset" Background="Transparent" BorderBrush="Transparent" >
                                <Image Source="pack://application:,,,/TSODD;component/images/delete.png" Width="10" Height="10"></Image >
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <EventSetter Event="Click" Handler="DeleteGroup"/>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                
            </DataGrid.Columns>
        </DataGrid>


      
        <Button x:Name="bt_Cancel" Grid.Row="3" HorizontalAlignment="Right" Content="Выход" Width="100" Height="20" Margin="0,0,10,10" Click="bt_Cancel_Click"></Button>
    



    </Grid>
</Window>
